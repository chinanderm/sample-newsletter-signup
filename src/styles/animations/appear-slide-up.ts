import { keyframes } from '@emotion/react'

const appearSlideUp = keyframes`
   0% {
      transform: translateY(15px);
   }
   100% {
      transform: translateY(0);
   }
`

export default appearSlideUp
